export default function listGifts(letter) {
  let obj = {};
  let loop = letter
    .split (' ')
 

    loop.forEach ((e, index) => {
      if (!e) return;
      if (e.includes ('_')) return;
      if (obj [e]) obj [e] +=  1;
      else obj [e] = 1;

      console.log (e)
    }); 
 
  // ¡Tú puedes!
  return obj;
}
 